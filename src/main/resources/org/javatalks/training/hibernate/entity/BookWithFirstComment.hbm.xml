<?xml version="1.0"?>
<!DOCTYPE hibernate-mapping PUBLIC
    "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
    "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">
<hibernate-mapping package="org.javatalks.training.hibernate.entity">
  <class name="BookWithFirstComment" subselect="select book.id id, book.title title,
         comment.id `comment`, comment.body `comment.body` from book
         left join comment on book.id=comment.id where comment.comment_order = 0">
    <id name="id">
      <generator class="native"/>
    </id>

    <property name="title"/>
    <many-to-one name="comment"/>
    <sql-query name="select * from book where id = ?" read-only="true" />

  </class>
</hibernate-mapping>
